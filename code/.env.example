# Optional local token file. Copy this file to `.env`, replace the placeholder,
# then uncomment the two clearly marked .env lines in bot.py.
# Never commit `.env`; it is intentionally ignored by Git.
BOT_TOKEN=replace_with_the_token_from_BotFather
